const BAD_REQUEST = 400;
const UNAUTHORIZED = 401; // Add this line
const NOT_FOUND = 404;
const INTERNAL_SERVER_ERROR = 500;
const CONFLICT = 409;
const FORBIDDEN = 403;

module.exports = {
  BAD_REQUEST,
  UNAUTHORIZED, // And this line
  NOT_FOUND,
  INTERNAL_SERVER_ERROR,
  CONFLICT,
  FORBIDDEN,
};
